@model IEnumerable<TestsProject.Models.Entities.TestParameters>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TimeLimit)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NumQuestions)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OpenFrom)
        </th>
	    <th>
		    @Html.DisplayNameFor(model => model.OpenTo)
	    </th>
	    <th>
		    @Html.DisplayNameFor(model => model.AllowedGroups)
	    </th>

        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TimeLimit)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NumQuestions)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OpenFrom)
        </td>
	    <td>
		    @Html.DisplayFor(modelItem => item.OpenTo)
	    </td>
	    <td>
		    @foreach (var group in item.AllowedGroups)
		    {
			    @Html.DisplayFor(g => group.Code)
			    <br/>
		    }
	    </td>
	    <td>
		    @Html.ActionLink("Add group", "NewGroup", new {id = item.Id}) |
			@Html.ActionLink("Edit topics", "NewTopic", new {id=item.Id}) |
		    @Html.ActionLink("Edit", "Edit", new {id = item.Id}) |
		    @Html.ActionLink("Details", "Details", new {id = item.Id}) |
		    @Html.ActionLink("Delete", "Delete", new {id = item.Id})
	    </td>
    </tr>
}

</table>
@Html.ActionLink("<<Back", "Index", "Teacher")